PORT=8787
NODE_ENV=production

# 至少 16 字符的随机字符串（生产必改）
JWT_SECRET=change-me-to-a-long-random-secret

# 用户数据目录（可选，默认 server/data）
# WEB_DATA_DIR=/var/lib/deepseek-agent

# 是否允许公开注册（false = 关闭注册）
ALLOW_REGISTER=true
